server:
  port: 9898

spring:
  application:
    name: IDENTITY-SERVICE

  datasource:
    url: jdbc:postgresql://localhost:5432/demo1
    username: postgres
    password: root123

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true


eureka:
  client:
    service-url:
      defaultZone : http://localhost:8761/eureka/
